apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namePrefix: pt-
resources:
  - locust.yaml
  - ../oidc-sinatra
  - ../load_testing

transformers:
  - |-
    apiVersion: builtin
    kind: NamespaceTransformer
    metadata:
      name: notImportantHere
      namespace: pt
    unsetOnly: true

